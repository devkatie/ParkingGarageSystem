package model;

public class Garage {
	/*
	 * a structure containing ParkingSpot objects, which are ordered so Vehicle
	 * objects can park into the correct sized ParkingSpot. There can only be
	 * one Garage object, which the User is not allowed to generate. The one and
	 * only garage will be initialized when the user creates an account, and the
	 * Garage object and all of the objects associated with it will be saved once
	 * the user logs out/closes the application.
	 */
	
	

}
